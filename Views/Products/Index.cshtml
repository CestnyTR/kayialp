@{
    ViewData["Title"] = "Products";
    Layout = "/Views/Shared/_Layout.cshtml";
}
<section class="position-relative bg-top-center overflow-hidden space-top" id="service-sec">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="title-area service-title-box text-center">
                    <h2 class="sec-title">Ürün Kategorileri</h2>
                    <p class="sec-text mb-50">İhtiyacınıza uygun kategorileri keşfedin.</p>
                </div>
            </div>
        </div>

        @await Component.InvokeAsync("CategoryList")
    </div>
</section>